<div class="navigation text-center">
    <ul class="pagination">
        {% if paginator.hasPreviousPage %}
            <li class="paginate_button page-item prev"><a href="{{ path('blog_index_paginated', {page: paginator.previousPage, tag: tagName}) }}" rel="previous"><i class="fa fw fa-long-arrow-left"></i> {{ 'paginator.previous'|trans }}</a></li>
        {% else %}
            <li class="paginate_button page-item prev disabled"><span><i class="fa fw fa-arrow-left"></i> {{ 'paginator.previous'|trans }}</span></li>
        {% endif %}

        {% for i in 1..paginator.lastPage %}
            {% if i == paginator.currentPage %}
                <li class="paginate_button page-item active"><span>{{ i }} <span class="sr-only">{{ 'paginator.current'|trans }}</span></span></li>
            {% else %}
                <li class="paginate_button page-item"><a href="{{ path('blog_index_paginated', {page: i, tag: tagName}) }}">{{ i }}</a></li>
            {% endif %}
        {% endfor %}

        {% if paginator.hasNextPage %}
            <li class="paginate_button page-item next"><a href="{{ path('blog_index_paginated', {page: paginator.nextPage, tag: tagName}) }}" rel="next">{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></a></li>
        {% else %}
            <li class="paginate_button page-item next disabled"><span>{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></span></li>
        {% endif %}
    </ul>
</div>
